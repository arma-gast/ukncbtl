#Совместимость -- какие программы в эмуляторе работают, проблемы

= Работает =

Операционные системы:
  * RT11SJ (DS) V5.01a (диск sysimage.dsk)
  * RT11SJ (S) V05.02 (sys1002.dsk)
  * ФОДОС Ф В03.00 (disk1.dsk)


= Проблемы =

НЕ работают программы:
  * NETUK  -- вылетает в СТОП!!!
  * FTS9   -- падает в СТОП на этапе передачи файла
  * Automatic links to other wiki pages
  * FMZ    (disk2.dsk) ?? программа форматирования дисков Formater MZ: V01.04, Oleg H., MIEPh, 1991 -- запускается, при размере сектора 256 ошибки форматирования!!!

НЕ работают игры:
  * AFGAN  (disk2.dsk) -- НЕ запускается!!! ?MON-F-Trap to 4 007476
  * DIGER  (disk2.dsk) -- НЕ запускается!!! TRAP TO 4 - FROM PC 056140
  * KOT    (disk2.dsk) ?? непонятно, запустилась ли??? ничего не выдает
  * LAND   (disk2.dsk) -- выдает белиберду на экран!!!
  * LAND1  (disk2.dsk) -- выдает белиберду на экран!!!
  * LAND2  (disk2.dsk) ?? непонятно, запустилась ли??? ничего не выдает
  * SHERIF (disk2.dsk) -- выдает белиберду на экран!!!
  * CAT    (game.dsk) ?? игра Кот-рыболов -- загружается, непонятно работает ли???
